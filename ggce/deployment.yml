---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: ggce
spec:
  selector:
    matchLabels:
      app: api
  replicas: 3
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: pdmm/ggce:17
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: shared-config
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db
  namespace: ggce
spec:
  selector:
    matchLabels:
      app: db
  serviceName: db
  replicas: 1
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - name: db
        image: postgres
        ports:
          - containerPort: 5432
        env:
          - name: POSTGRES_USER
            valueFrom:
              configMapKeyRef:
                name: shared-config
                key: DB_USER
          - name: POSTGRES_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: shared-config
                key: DB_PASS
          - name: POSTGRES_DB
            valueFrom:
              configMapKeyRef:
                name: shared-config
                key: DB_NAME

